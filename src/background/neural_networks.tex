Recently artificial neural networks have made a vast comeback in research.
As computing power has increased dramatically, more complex neural networks have became feasible for different applications.
Much success in using neural networks has been found in e.g.\ image classification, speech and text recognition, object detection, and automation of different tasks.

Originally the early concepts of neural networks were already presented in the 1940s~\cite{mcculloch, hebb} as a mathematical model for nervous activity.
In the 1950s to 1970s the first concepts were modelled with a single neuron but the application of backpropagation enabled optimising multiple layers of neurons that started the research of modern neural networks.
Then in to 2000's the latest wave of neural network research was made possible by powerful parallel computing performance that made it possible to train networks for learning ever more complex phenomena.

Next some basics of neural networks are covered starting from the McCulloch-Pitts Neuron and training it to backpropagation to the modern convolutional neural networks needed for the purpose of this work.
All material in this section is pretty standard neural networks basics and is not completely necessary for understanding the basic concept of the final model.
However, a reader previously unfamiliar with neural networks basics might find e.g.~\cite{goodfellow} helpful, from which most of this material is taken from, helpful to understand the cocepts better.

\subsubsection{Neurons and Activation Functions}
The first mathematical model for neuron from~\cite{mcculloch}, known as the McCulloch-Pitts Neuron is the first building block for any neural networks.
It consists of aggregating an input vector for the neuron, and of activation that makes the decision based on the aggregation.
The McCulloch-Pitts Neuron is depicted in figure~\ref{fig:mccullochpittsneuron}.
\begin{figure}\label{fig:mccullochpittsneuron}
    \includegraphics{background/fig/mccullochpitts.png}
    \caption{McCulloch-Pitts neuron (placeholder image)}
\end{figure}
The McCulloch-Pitts Neuron takes an $n$-dimensional binary vector $\mathbf{x}$ as input, and applies function $f : \R^n \rightarrow \R$, a sum of the inputs
\begin{equation}
    f(\mathbf{x}) = \sum_{i=1}^{n} x_{i}.
\end{equation}
Function $\phi$ is then the binary activation function determining the output of the neuron given some threshold $T$,
\begin{align}
    y = \phi(f(\mathbf{x})) & = 1 \text{ if } \phi(\mathbf{x}) \geq T \\
                            & = 0 \text{ if } \phi(\mathbf{x}) < T. 
\end{align}

This model has the difficulty of simplicity.
Only binary input vectors $\mathbf{x}$ are allowed, and the output is always binary.

A few years later Donald Hebb~\cite{hebb} proposed that the proximity of inputs and other neurons should affect the output as well.
This concept was applied to the neuron model by Frank Rosenblatt~\cite{rosenblatt} who developed the perceptron that is much more flexible model.
Now the inputs can be any values and they are weighted with a weight vector $\bm{w}$, and there is also a bias term in $f$.
So now function $f : \R^n \rightarrow \R$ is
\begin{equation}
    f(\mathbf{x}) = \sum_{i=1}^{n} w_{i} x_{i} + b.
\end{equation}
The perceptron is depicted in figure~\ref{fig:perceptron}.

\begin{figure}\label{fig:perceptron}
    \includegraphics{background/fig/mccullochpitts.png}
    \caption{Perceptron (placeholder image)}
\end{figure}

The activation function $\phi$ can also be modified to be a more sophisticated one than the binary step function.
Typical activation functions used in neural networks include the Rectified Linear Unit (ReLU) and the Leaky ReLU, the Sigmoid function, and the Softmax function.
The mentioned activation functions are shown in figure~\ref{fig:activations}.
The choice of activation function comes with technicalities that are important in training a neural networks including the possible range of values and behavior of their derivatives.

\begin{figure}\label{fig:activations}
    \includegraphics{background/fig/activations.png}
    \caption{Activation functions (placeholder image)}
\end{figure}

The single neuron model can only be used for extremely simple tasks.
Therefore the neurons were started to be combined as layers to create something called Multilayer Perceptron (MLP).
MLP is nothing more than combining a number of neurons in layers and applying them sequentially as depicted in figure~\ref{fig:mlp}.
\begin{figure}\label{fig:mlp}
    \includegraphics{background/fig/activations.png}
    \caption{Multilayer Perceptron (placeholder image)}
\end{figure}

Formally the MLP function $f^{(l)} : \R^n \rightarrow \R^m$ for layer $l$ is now
\begin{equation}
    f^{(l)}(\bm{x}; \bm{W}, \bm{b}) = \bm{W} \bm{x} + \bm{b},
\end{equation}
where rows of the weight matrix $\bm{W} \in \R^{n \times m}$ has now the weights and bias vector $\bm{b} \in \R^{m}$ has the bias terms of the neurons in the layer.
The activation function is now applied elementwise for the outputs of $f^{(l)}$.
The whole network is then a composition of all the layers in the network.

MLPs are related to the important Universal Approximation Theorem presented at the turn of the 1990's~\cite{cybenko,horniketal,hornik} that states that these MLP networks with at least one hidden layer and any squashing activation function can approximate any Borel measurable function between finite-dimensional spaces.
While the theorem might sound compelling, that the the number of neurons is not necessarily bounded for all functions even though there are some boundedness results for some classes of functions.
This property may thus lead to unfeasibly large network.
Moreover, the theorem does not state that any suitable function can be learnt, only represented.

In MLP all the neurons in the layers are connected to each other but for a general neural network it is not mandatory, a convolutional one that is the main interest of this work being an example of a partially connected network.

\subsubsection{Convolutional Neural Networks}
Convolutional Neural Networks (CNNs) are a type of neural networks that have been recently popular and widely studied in e.g.\ image classification tasks.
The main power of CNNs is that they have proved to be effective in finding the essential structures especially from image data.
While the basic concept is similar to the MLP model, CNN is quite different in its working principles, and thus presented in detail here to understand how the model of this work works.

The $n$-dimensional convolution of two compactly supported functions $f, g : \R^n \rightarrow \R$ is defined by
\begin{equation}
    (f * g)(x) = \int_{\R^n} f(y) g(x-y) \, \mathrm{d}y.
\end{equation}

With neural networks operations need to be discretized so the convolution for compactly supported $f, g : \Z^n \rightarrow \R$ becomes
\begin{equation}
    (f * g)(x) = \sum_{y \in \N^{n}} f(y) g(x-y).
\end{equation}
And the dimension of convolution is in practice mostly at most 3 and especially with image data two-dimensional convolutions are used and thus the examples from here on concern the two-dimensional case.
Even though with a simple change of variables it is easy to see that the convolution is commutative, in neural networks $f$ is usually thought to be some input data and $g$ a convolution kernel.
However, practical neural network libraries implement a similar cross-correlation function~\cite{goodfellow}
\begin{equation}
    (f * g)(x) = \sum_{y \in \N^{n}} f(x) g(y+x),
\end{equation}
in which the kernel is not flipped resulting in losing the commutative property.
This doesn't affect the end result as by flipping the convolution kernel the cross-correlation function has the same result.
Thus this is only a technical detail and the layers are nevertheless called convolution layers instead of cross-correlation layers.

In CNNs the dot-product neurons in the layers of MLP are now replaced with many small convolutions operations.
With images the convolution kernels have often sizes of $3 \times 3$ or $9 \times 9$, and convolutions are computed with a large number of these small kernels to the image input.
Many convolution kernels are approximated, and the input is convoluted with many convolution filters individually to increase the depth of input.
A typical number of convolution filters would be 32, or 64.
The idea is depicted in figure~\ref{fig:2dconv}.
\begin{figure}\label{fig:2dconv}
    \includegraphics{background/fig/activations.png}
    \caption{2d convolution (placeholder image)}
\end{figure}

Between convolution layers the dimensions of the input is usually downsampled by pooling layers which combine multiple values to one usually by taking average (average pooling) or maximum (max pooling) values.
After convolution layers there some activation functions are usually used, and also some normalisations for the data in preparation for next layers.
Convolutions with pooling and normalisations are rarely the only type of layers in CNN architectures and typically at least the very last layers are similar fully connected dot-product layers as in the MLP\@

\subsubsection{Optimising Neural Network Parameters}

Computing a large number of these numerical operations with arbitrary parameters have little use in practice.
Therefore a neural network has to unergo a training process during which the parametrs are optimised over a large training dataset.

Classically these learning tasks are divided to supervised, unsupervised, and reinforcment learning.
As the name implies, in supervised learning tasks there is some supervision which means that the training data is labelled such that for every input there is a target output for which a prediction is compared to.
In unsupervised learning the data is not labelled and the unsupervised model is trained to learn e.g.\ some structures from the data.
Combinations of supervised and unsupervised learning also exist, called semi-supervised learning.
An example would be various forms of clustering tasks.
In reinforcment learning a model is trained to take some actions in an environment from which it gets reward or punishment based on taken actions.
Then the goal is to maximise the cumulative reward of all actions taken.
A semirecent example of reinforcment learning is AlphaGo, a model trained to play the board game Go.
In this example the optimal action taken at each step of the game would be impossible to figure out so the model has to figure out the best moves against another player to win the game.

But all these learning tasks with neural network come down to an optimisation problem.
Training a neural network is related to any other numerical optimisation methods but due to the huge number of parameters needed and non-convex nature of the optimisation problems, in practice training is a long and complicated task.
The optimisation problem of a supervised learning task can be written out as a minimisation of a real-valued const functional $J$, that is the expected value of a loss function, with respect to network parameters $\bm{\theta}$:
\begin{equation}
    \min_{\bm{\theta}} \, J(\bm{\theta}) = \mathbb{E}_{(\bm{x},y) \sim \hat{p}_{\text{data}}} L(f(\bm{x}; \bm{\theta}), y) = \frac{1}{n} \sum_{i=1}^{n}  L(f(\bm{x}^{(i)}; \bm{\theta}), y^{(i)}).
\end{equation}
Here $f$ is now the neural network function that gives some prediction given input data $\bm{x}$ coming from the empirical training data distribution, and the network parameters.
$L$ is the loss function comparing the network prediction, and the true label $y$.
This is easily modified to unsupervised learning by simply excluding $y$ from the equation.

The choice of loss function is a detail that must be chosen for training.
The family of loss functions suitable for neural networks is large and depends on the data values.
While the definitions of different loss functions look very different, typically the choice is made based on how much large prediction errors should be compared to small errors.
Perhaps the most used and well-known loss function is the mean squared error (MSE) that takes the mean of squared prediction errors,
\begin{equation}
    L_{\text{MSE}}(Y, \hat{Y}) = \frac{1}{n} \sum_{i=1}^{n} \left( Y_{i} - \hat{Y}_{i} \right)^{2}.
\end{equation}
Now $Y$ is the target value and $\hat{Y}$ the predicted value.

Most optimisation algorithms with neural networks are, or are based on the Stochastic gradient descent (SGD) which is a derivative of the classic gradient descent algorithm.
The gradient descent algorithm works simply by moving the decision variable to the direction of the gradient of the loss function multiplied by some small number called step size.
The stochastic version primarily differs from the original one by estimating gradients from a subset of the data reducing the computational cost.
With actual neural networks computing gradients of the objective function for the entire training dataset is not practical and therefore the gradients has to be computed for small samples from the training data.
For a true stochastic optimisation method only one sample would be taken but it is often more practical to take a small number of sampled and this is called minibatching.
The SGD algorithm with minibatching is laid out in detail in Algorithm~\ref{alg:sgd}.

\begin{algorithm}[H]
    \caption{Stochastic gradient descent}
    \label{alg:sgd}
    \begin{algorithmic}[1]
        \State\textbf{initialise} step size $\eta > 0$, initial parameter $\bm{\theta}$
        \While{stopping criterion not met} \Comment{E.g.\ non-decreasing losses}
        \State Sample a minibatch from training dataset $\{ (\bm{x}^{(i)}, y^{(i)}) \}_{i=1}^{m}$
        \State $\bm{d} = -\frac{1}{m} \nabla_{\bm{\theta}} \sum_{i=1}^{m} L(f(\bm{x}^{(i)}; \bm{\theta}), y^{(i)})$ \Comment{Compute search direction}
        \State $\bm{\theta} = \bm{\theta} + \eta \bm{d}$ \Comment{Update parameters}
        \EndWhile
        \State\textbf{return} $\bm{\theta}$
    \end{algorithmic}
\end{algorithm}

Due to the random sampling of minibatches there is some noise in the computed losses.
Therefore in practice the step size needs to be decreased in training at some rate since that noise occurs also in the optimum.
Many techniques exist for step size scheduling and while used also in training the model of this thesis but is mostly a technical detail and for the interested~\cite{goodfellow} provides some deeper discussions on the matter.

More sophisticated optimisation algorithms are also often used in optimising neural networks.
A simple addition to the SGD algorithm would SGD with momentum.
Momentum addition would mean adding previous search directions to just the most recent gradients with some weights.
Some other popular optimisation algorithms used for neural network training are RMSProp, AdaDelta, and Adam.
Again,~\cite{goodfellow} provides detailed explanations of many other algorithms.
The different algorithms have each some benefits, for example some don't have the issue of scheduling step sizes as with SGD but there is no universal algorithm that would be best for all cases.
However, the idea of moving parameters towards the gradient of a loss function is the concept that unites these all.
Therefore the next problem that rises is how to compute the gradients.

\subsubsection{Backpropagation}

Training a neural network starts with computing predictions of the network using some inputs from training dataset.
This part if often called forward propagation.
However, in all optimisation algorithms at some point the gradient of a scalar loss function.
This part is called backpropagation.
Numerically computing gradients in the classical way for neural networks consisting a huge number of parameters is computationally infeasible and thus some other methods are required to train a network in practise.

Reverse accumulation as an automatic differentiation method for MLP networks was first introduced in the 1970's and 1980's and was to be called backpropagation.
Backpropagation was first introduces by Paul Werbos in their PhD thesis from 1974 in~\cite{werbos} but was made famous a decade later by David Rumelhart and others~\cite{rumelhartetal}.
These breakthroughs made it now possible to efficiently train more complex networks due to the efficient gradient estimation of neural network parameters.
But reverse accumulating gradients was studied already independently by multiple others.
Perhaps the most notable example being the works of the Finnish mathematician Seppo Linnainmaa in their MSc thesis from 1970~\cite{linnainmaa} though the concept of reverse accumulation was studied in cumulative floating point errors.

The basic principle behind the backpropagation algorithm is the recursive use of the chain rule of calculus.
The chain rule states that for $g: \R^{m}\rightarrow \R^{n}$ and $f: \R^{n} \rightarrow \R$ with $f(g(\bm{x}))$ the partial derivatives of $f$ are given by
\begin{equation}
    \frac{\partial f}{\partial \bm{x}_{i}} = \sum_{j = 1}^{n} \frac{\partial f}{\partial (g(\bm{x})_{j})} \frac{\partial (g(\bm{x})_{j})}{\partial x_{i}}.
\end{equation}

Now the objective function $J$ of the optimisation problem can be thouth to be a composition of all the individual functions in the network and then the chain rule can be used to obtain the partial derivatives with respect to the trainable network parameters.

As the shapes and sizes of the trainable parameters may be different for each specific network architecture, the exact formulation of the gradients might differ a bit.
For the simple MLP case the formulas are easy to write to see what is the idea behind the backpropagation algorithm.
Recall that the network parameters can be expressed for each layer $\ell$ as a matrix $\bm{\theta}^{(\ell)} = \begin{bmatrix} \bm{W} & \bm{b} \end{bmatrix}^{(\ell)}$. Then mark the output of layer $\ell$ as $\bm{z}^{(\ell)} = \bm{W}^{(\ell)} \bm{a}^{(\ell-1)} + \bm{b}$ where $\bm{a}^{(\ell-1)} =  \phi^{\ell-1}(\bm{z}^{(\ell-1)})$ is the output from the activation of the previous layer.
Now the chain rule thus gives the
\begin{equation}\label{eq:mlpchainrule}
    \frac{\partial J}{\partial \bm{\theta}_{ij}^{(\ell)}} = \sum_{k} \frac{\partial J}{\partial \bm{z}_{k}^{(\ell)}} \frac{\partial \bm{z}_{k}^{(\ell)}}{\partial \bm{\theta}_{ij}^{(\ell)}}. 
\end{equation}
Now the $\bm{z}$ derivatives are easily evaluated for the weight terms to
\begin{equation}
    \frac{\partial J}{\partial \bm{W}_{ij}^{(\ell)}} = \frac{\partial J}{\partial \bm{z}_{j}^{(\ell)}} \bm{z}_{i}^{(\ell)},
\end{equation}
and for the bias terms to
\begin{equation}
    \frac{\partial J}{\partial \bm{b}_{j}^{(\ell)}} = \frac{\partial J}{\partial \bm{z}_{j}^{(\ell)}} 1.
\end{equation}
Thus~\eqref{eq:mlpchainrule} can now be written as
\begin{equation}\label{eq:mlchainrulevec}
    \bm{\nabla}_{\bm{\theta}^{(\ell)}} J = \begin{bmatrix} \bm{x}^{(\ell)} \\ 1 \end{bmatrix} (\nabla_{\bm{z}^{(\ell)}} J)^{\top}.
\end{equation}
What is left now is to find an equation for the gradient term of~\eqref{eq:mlchainrulevec} that is often called the local gradient or error is usually denoted by $\bm{\delta}^{(\ell)}$.
By applying the chain rule once again,
\begin{align}
    \delta_{j}^{(\ell)} = \frac{\partial J}{\partial \bm{z}_{j}^{(\ell)}} = &\sum_{k} \underbrace{\frac{\partial J}{\partial \bm{z}_{k}^{(\ell + 1)}}}_{=\delta_{k}^{(\ell+1)}} \frac{\partial \bm{z}_{k}^{(\ell+1)}}{\partial \bm{z}_{j}^{(\ell)}} \\
                                                                       &= \sum_{k} \delta_{k}^{(\ell+1)} \frac{\partial \bm{z}_{k}^{(\ell+1)}}{\partial \bm{z}_{j}^{(\ell)}},
\end{align}
from which
\begin{align}
    \frac{\partial \bm{z}_{k}^{(\ell+1)}}{\partial \bm{z}_{j}^{(\ell)}}  = \frac{\partial}{\partial \bm{z}_{j}^{(\ell)}} \sum_{i} \bm{W}_{ki}^{(\ell+1)} \phi^{(\ell)}(\bm{z}_{i}^{(\ell)}) + \bm{b}_{k}^{(\ell+1)} = \bm{W}_{kj}^{(\ell+1)} \phi^{(\ell)\prime} (\bm{z}_{j}^{(\ell)}).
\end{align}
Thus for the error is now recursively
\begin{equation}
    \delta_{j}^{(\ell)} =  \sum_{k} \delta_{k}^{(\ell+1)} \bm{W}_{kj}^{(\ell+1)} \phi^{(\ell)\prime} (\bm{z}_{j}^{(\ell)}).
\end{equation}
For the last layer $L$ the error can be given more simply,
\begin{align}
    \delta_{j}^{(L)} = \frac{\partial J}{\partial \bm{z}_{j}^{(L)}} &= \sum_{k} \frac{\partial J}{\partial \bm{a}_{k}^{L}} \underbrace{\frac{\partial \bm{a}_{k}^{L}}{\partial \bm{z}_{j}^{L}}}_{=0\text{ when } k \neq j} \\
                                                                    &= \frac{\partial J}{\partial \bm{a}_{j}^{L}} \frac{\partial \bm{a}_{j}^{L}}{\partial \bm{z}_{j}^{L}}\\
                                                                    &= \frac{\partial J}{\partial \bm{a}_{j}^{L}} \phi^{(L)\prime} (\bm{z}_{j}^{(L)}).
\end{align}

Now that the neural network can be constructed, there is a way to find the optimal network parameters with computing easily the gradients, a complete neural network can be trained.
Typically most of these things presented are written inside the numerical neural network libraries the most well known examples being probably PyTorch and Tensorflow.
What is left for the user is to define a network that can be used for the task in hand.
Finding the best architecture for a specific task can be difficult and there is a lot of active research in creating the optimal networks for general tasks that can be applied when building a network.

